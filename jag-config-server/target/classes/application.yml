server:
  port: 7070

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Alexjhona/Microservicios-exam-alex.git
          searchPaths: config-data
          default-label: main
  security:
    user:
      name: Alex
      password: root
